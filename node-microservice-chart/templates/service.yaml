apiVersion: v1
kind: Service
metadata:
  name: {{ include "node-microservice-chart.fullname" . }}
  labels:
    {{- include "node-microservice-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "node-microservice-chart.selectorLabels" . | nindent 4 }}
