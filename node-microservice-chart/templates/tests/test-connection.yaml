apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "node-microservice-chart.fullname" . }}-test-connection"
  labels:
    {{- include "node-microservice-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "node-microservice-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
